#include "main.h"
#include<stdlib.h>

/**
 *int_abs - computes absolute value of an integer
 *@n: integer whose absolute value is to be returned
 *Return: returns the absolute value
 */

int_abs(int n)
{
	return (abs(n));
}
